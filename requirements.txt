# ARENA runtime dependencies.
# Grown per module - see modular-plan.md. Currently: M1.

# M1 - config, tools, scenarios
numpy>=2.0
pydantic>=2.0
PyYAML>=6.0

# M3 - environment
gymnasium>=0.29
pettingzoo>=1.24

# M4 - baselines
scikit-learn>=1.3

# M5 - policies + PPO
torch>=2.2

# tests
pytest>=8.0

# M9 - dataset fetch: no new deps. Uses stdlib urllib/json/hashlib/tarfile and
#      the HF datasets-server rows API (JSON), not parquet. See THIRD_PARTY.md.

# --- added in later modules ---
matplotlib>=3.8
ollama>=0.4
