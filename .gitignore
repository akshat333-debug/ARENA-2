__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store

# fetched datasets - never vendored (see project.md S13). Anchored to repo root
# so it does NOT swallow the arena/data/ source package.
/data/

# training artifacts
runs/
checkpoints/
*.pt
*.ckpt

# llm response cache
.cache/

.venv/
venv/
.env

# long-running experiment outputs (regenerate with scripts/run_multiseed.py)
report/paper_run/
